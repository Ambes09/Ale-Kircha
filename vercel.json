{
  "version": 2,
  "builds": [
    {
      "src": "apps/customer-web/package.json",
      "use": "@vercel/static-build",
      "config": {
        "buildCommand": "cd apps/customer-web && npm install && npm run build",
        "outputDirectory": "apps/customer-web/dist"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/apps/customer-web/dist/$1"
    }
  ],
  "env": {
    "VITE_API_URL": "https://ale-kircha-api.onrender.com"
  }
}
