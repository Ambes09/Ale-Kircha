generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// The rest of your schema remains the same...
// (Copy your existing model definitions here)
